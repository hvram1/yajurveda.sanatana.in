---
import BaseLayout from '../layouts/BaseLayout.astro';
import KandaCard from '../components/KandaCard.astro';
import { getStructureIndex, KANDA_NAMES } from '../lib/utils';

const structure = getStructureIndex();
---

<BaseLayout title="मुख्यपृष्ठम्">
  <div class="text-center mb-12">
    <h1 class="text-3xl md:text-5xl text-indigo mb-4">कृष्ण यजुर्वेद</h1>
    <p class="text-lg text-gray-600">तैत्तिरीय संहिता</p>
    <p class="text-base text-gray-500 mt-2">Krishna Yajurveda - Taittiriya Samhita</p>
  </div>

  <div class="fancy mb-8">
    <span>॥</span>
  </div>

  <!-- Stats -->
  <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-12">
    <div class="verse-card text-center">
      <div class="text-3xl font-bold text-primary">{structure.kandas.length}</div>
      <div class="text-sm text-gray-600">काण्डानि</div>
      <div class="text-xs text-gray-500">Kandas</div>
    </div>
    <div class="verse-card text-center">
      <div class="text-3xl font-bold text-primary">
        {structure.kandas.reduce((sum, k) => sum + k.prasnas.length, 0)}
      </div>
      <div class="text-sm text-gray-600">प्रश्नाः</div>
      <div class="text-xs text-gray-500">Prasnas</div>
    </div>
    <div class="verse-card text-center">
      <div class="text-3xl font-bold text-primary">
        {structure.kandas.reduce((sum, k) => 
          sum + k.prasnas.reduce((ps, p) => ps + p.anuvakas.length, 0), 0)}
      </div>
      <div class="text-sm text-gray-600">अनुवाकाः</div>
      <div class="text-xs text-gray-500">Anuvakas</div>
    </div>
    <div class="verse-card text-center">
      <div class="text-3xl font-bold text-primary">
        {structure.kandas.reduce((sum, k) => 
          sum + k.prasnas.reduce((ps, p) => 
            ps + p.anuvakas.reduce((as, a) => as + a.panchasatCount, 0), 0), 0)}
      </div>
      <div class="text-sm text-gray-600">पञ्चाशत्</div>
      <div class="text-xs text-gray-500">Verses</div>
    </div>
  </div>

  <!-- Kanda Cards -->
  <section class="mb-12">
    <h2 class="text-2xl text-indigo mb-6 text-center">काण्डानि (Kandas)</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {structure.kandas.map(kanda => (
        <KandaCard 
          kanda={kanda.id} 
          prasnaCount={kanda.prasnas.length} 
        />
      ))}
    </div>
  </section>

  <!-- About section -->
  <section class="verse-card mt-12">
    <h2 class="text-xl text-indigo mb-4">तैत्तिरीय संहिता विषये</h2>
    <div class="text-gray-700 space-y-4">
      <p>
        तैत्तिरीय संहिता कृष्ण यजुर्वेदस्य प्रमुखा शाखा अस्ति। 
        अस्य नाम तित्तिरि नामकात् ऋषेः प्राप्तम्।
      </p>
      <p>
        The Taittiriya Samhita is a major recension of the Krishna (Black) Yajurveda. 
        It is named after the sage Tittiri, who was a student of Yaska.
      </p>
      <p>
        This text is organized into 7 Kandas (books), 44 Prasnas (chapters), 
        651 Anuvakas (sections), and 2198 Panchasats (verse groups).
      </p>
    </div>
  </section>
</BaseLayout>
