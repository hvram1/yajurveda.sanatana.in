---
import BaseLayout from '../../layouts/BaseLayout.astro';
import fs from 'fs';
import path from 'path';

const base = import.meta.env.BASE_URL;
const taBase = `${base}ta/`;

// Load TA metadata
const metadataPath = path.join(process.cwd(), 'src/data/ta/metadata.json');
const metadata = JSON.parse(fs.readFileSync(metadataPath, 'utf-8'));

// Calculate totals
const totalAnuvakas = metadata.prapatakas.reduce((sum, p) => sum + p.anuvakas.length, 0);
const totalPanchasats = metadata.prapatakas.reduce((sum, p) => 
  sum + p.anuvakas.reduce((as, a) => as + a.panchasats, 0), 0);
---

<BaseLayout title="तैत्तिरीय आरण्यक" activeText="ta">
  <div class="text-center mb-8">
    <h1 class="text-3xl md:text-4xl text-indigo mb-4">तैत्तिरीय आरण्यक</h1>
    <p class="text-lg text-gray-600">Taittiriya Aranyaka</p>
  </div>

  <div class="fancy mb-8">
    <span>॥</span>
  </div>

  <!-- Stats -->
  <div class="grid grid-cols-2 md:grid-cols-3 gap-4 mb-12">
    <div class="verse-card text-center">
      <div class="text-3xl font-bold text-purple-600">{metadata.prapatakas.length}</div>
      <div class="text-sm text-gray-600">प्रपाठकाः</div>
      <div class="text-xs text-gray-500">Prapatakas</div>
    </div>
    <div class="verse-card text-center">
      <div class="text-3xl font-bold text-purple-600">{totalAnuvakas}</div>
      <div class="text-sm text-gray-600">अनुवाकाः</div>
      <div class="text-xs text-gray-500">Anuvakas</div>
    </div>
    <div class="verse-card text-center">
      <div class="text-3xl font-bold text-purple-600">{totalPanchasats}</div>
      <div class="text-sm text-gray-600">पञ्चाशत्</div>
      <div class="text-xs text-gray-500">Panchasats</div>
    </div>
  </div>

  <!-- Prapaataka Cards -->
  <section class="mb-12">
    <h2 class="text-2xl text-indigo mb-6 text-center">प्रपाठकाः (Prapatakas)</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
      {metadata.prapatakas.map(prapaataka => {
        const anuvCount = prapaataka.anuvakas.length;
        const panchCount = prapaataka.anuvakas.reduce((sum, a) => sum + a.panchasats, 0);
        
        return (
          <a href={`${taBase}prapaataka/${prapaataka.prapaataka}`} class="prapaataka-card group">
            <div class="prapaataka-header bg-gradient-to-br from-purple-500 to-purple-700">
              <div class="prapaataka-number">{prapaataka.prapaataka}</div>
              <h3 class="text-lg font-bold">प्रपाठकः {prapaataka.prapaataka}</h3>
            </div>
            <div class="prapaataka-body">
              <div class="stat-row">
                <span class="stat-label">अनुवाकाः:</span>
                <span class="stat-value">{anuvCount}</span>
              </div>
              <div class="stat-row">
                <span class="stat-label">पञ्चाशत्:</span>
                <span class="stat-value">{panchCount}</span>
              </div>
              <div class="view-link group-hover:underline">अवलोकनम् →</div>
            </div>
          </a>
        );
      })}
    </div>
  </section>

  <!-- About section -->
  <section class="verse-card mt-12">
    <h2 class="text-xl text-indigo mb-4">आरण्यक विषये</h2>
    <div class="text-gray-700 space-y-4">
      <p>
        तैत्तिरीय आरण्यकं वनेषु अध्येतव्याः रहस्यात्मकाः शिक्षाः समाविशति।
        अस्मिन् उपनिषदः, ध्यानविधयः, आत्मज्ञानं च निरूपितम्।
      </p>
      <p>
        The Taittiriya Aranyaka contains esoteric teachings meant to be studied in forests.
        It includes the famous Taittiriya Upanishad, meditation techniques, and knowledge of the Self.
        Prapatakas 7-9 contain the Taittiriya Upanishad with its teachings on Brahman and the five sheaths.
      </p>
      <div class="mt-4 p-4 bg-purple-50 rounded-lg">
        <h3 class="font-semibold text-purple-800 mb-2">Notable Sections:</h3>
        <ul class="text-sm space-y-1 text-purple-700">
          <li>• प्रपाठकः 7-9: तैत्तिरीय उपनिषद् (Shiksha Valli, Ananda Valli, Bhrigu Valli)</li>
          <li>• प्रपाठकः 10: महानारायण उपनिषद्</li>
        </ul>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .prapaataka-card {
    display: block;
    border-radius: 0.5rem;
    overflow: hidden;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    transition: transform 0.2s, box-shadow 0.2s;
    background: white;
  }
  
  .prapaataka-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
  }
  
  .prapaataka-header {
    padding: 1rem;
    color: white;
    text-align: center;
  }
  
  .prapaataka-number {
    width: 40px;
    height: 40px;
    background: rgba(255,255,255,0.2);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.25rem;
    font-weight: bold;
    margin: 0 auto 0.5rem;
  }
  
  .prapaataka-body {
    padding: 0.75rem;
  }
  
  .stat-row {
    display: flex;
    justify-content: space-between;
    padding: 0.25rem 0;
    border-bottom: 1px solid #f0f0f0;
  }
  
  .stat-row:last-of-type {
    border-bottom: none;
  }
  
  .stat-label {
    color: #666;
    font-size: 0.8rem;
  }
  
  .stat-value {
    font-weight: 600;
    color: #333;
    font-size: 0.9rem;
  }
  
  .view-link {
    margin-top: 0.5rem;
    text-align: center;
    color: var(--color-primary);
    font-size: 0.8rem;
  }
</style>
